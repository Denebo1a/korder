问题仍未解决，让我们用更高的要求先修改程序的其他部分：
（1）允许每个片段设定拍号，并根据拍号来渲染小节块（影响每小节拍数）
（2）允许用户全局设置量化级别（四分音符、八分音符、十六分音符），量化级别会影响小节内横轴的最小分度（现在默认为四分音符，即4/4拍号下的一拍，若改为八分音符，则要再在一个四分音符内添加一条分割线），但是只有拍开头处需要标明数字（如现在的1、2、3、4），修改样式使每拍的开头位置更加醒目
（3）请保留原有的样式，使用在视觉上稍微上浮晃动的特效来显示拖拽时的样式。并将编辑栏的触发改为通过双击和声块打开
（4）为和声块添加右边缘拖拽手柄，实现直观的长度调整功能，并在拖拽时实时显示和声块持续时间
（5）请修改add-harmony-btn的样式，使其显示在和声块右侧居中，但是保持一定距离，防止与右部拖拽手柄重叠
（6）请将空格键修改为播放/暂停的快捷键，并且取消一些可能由使用空格键引起误操作默认行为。